FROM ${BASE_IMAGE}
COPY ${APP_FILE} /app/alpha-functions-sdk.jar
COPY ${ALPHA_FUNCTION_EXECUTABLE_PATH} /app/app.jar
WORKDIR /app
ENTRYPOINT ["${ENTRY_COMMAND}","${ENTRY_COMMAND_OPTION}", "${ENTRY_ARGS}","${MAIN_FUNCTION_PATH}", "${EXECUTION_HANDLER}"]